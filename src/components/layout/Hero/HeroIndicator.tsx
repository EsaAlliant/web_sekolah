export function HeroIndicator({ count, activeIndex, onSelect }: { count: number; activeIndex: number; onSelect: (index: number) => void }) { return <div className="hero-indicators" role="tablist" aria-label="Pilih slide">{Array.from({ length: count }, (_, index) => <button aria-label={`Tampilkan slide ${index + 1}`} aria-selected={index === activeIndex} className={index === activeIndex ? "is-active" : ""} key={index} onClick={() => onSelect(index)} role="tab" type="button" />)}</div>; }
