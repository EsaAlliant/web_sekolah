import { PageHeader } from "./PageHeader";
import { Section } from "./Section";
export function PlaceholderPage({ title }: { title: string }) { return <Section><PageHeader title={title} description="Halaman placeholder untuk fase fondasi." /></Section>; }
